{
  "name": "localdevhub",
  "version": "1.0.0",
  "description": "A platform connecting local developers with NGOs and SMEs for software projects",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "build": "cd client && npm run build",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install",
    "render-build": "npm install && cd server && npm ci && cd ../client && npm ci && npm run build"
  },
  "keywords": [
    "react",
    "nodejs",
    "mongodb",
    "developer",
    "ngo",
    "sme"
  ],
  "author": "LocalDevHub Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "mongodb": "^6.20.0"
  }
}
